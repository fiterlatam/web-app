import {FormatDisplayNamePipe} from "./format-display-name.pipe";

describe('FormatDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
