import {FormatIntegerDirective} from './format-integer.directive';

describe('FormatNumberDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new FormatIntegerDirective();
    expect(directive).toBeTruthy();
  });
});
