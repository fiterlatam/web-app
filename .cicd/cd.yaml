trigger:
  batch: true
  tags:
    include:
      - v*

pr: none

resources:
  repositories:
    - repository: self
    - repository: common
      type: git
      name: "Incursor/incursor-back"
      ref: refs/heads/feat/ynorena/fineract-ci-cd

variables:
  - template: .cicd/variables/common.yaml@common

stages:
  - template: .cicd/stages/fineract/backend/cd.yaml@common